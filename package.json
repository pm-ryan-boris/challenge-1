{
  "dependencies": {
    "@babel/preset-env": "^7.3.1",
    "babel-core": "^6.26.3",
    "babel-jest": "^24.0.0",
    "babel-preset-env": "^1.7.0",
    "jest": "^24.0.0",
    "jest-cli": "^24.0.0"
  },
  "scripts": {
    "test": "jest --watch --verbose"
  }
}
